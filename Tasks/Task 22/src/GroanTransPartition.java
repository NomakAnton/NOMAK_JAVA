public abstract class GroanTransPartition extends Transport implements CanRide {

    private static final int DEFAULT_SIZE = 1;

    public GroanTransPartition(int number,int size) {
        super(number,size);
    }

}
